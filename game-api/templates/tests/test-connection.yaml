apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "game-api.fullname" . }}-test-connection"
  labels:
    {{- include "game-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "game-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

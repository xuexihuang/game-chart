apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gameserver-mines.fullname" . }}-test-connection"
  labels:
    {{- include "gameserver-mines.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gameserver-mines.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

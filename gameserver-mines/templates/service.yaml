apiVersion: v1
kind: Service
metadata:
  name: {{ include "gameserver-mines.fullname" . }}
  labels:
    {{- include "gameserver-mines.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gameserver-mines.selectorLabels" . | nindent 4 }}

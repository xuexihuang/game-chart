apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-rpc.fullname" . }}-test-connection"
  labels:
    {{- include "admin-rpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-rpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gameserver-lobby.fullname" . }}-test-connection"
  labels:
    {{- include "gameserver-lobby.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gameserver-lobby.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

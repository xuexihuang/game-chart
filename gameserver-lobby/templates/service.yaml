apiVersion: v1
kind: Service
metadata:
  name: {{ include "gameserver-lobby.fullname" . }}
  labels:
    {{- include "gameserver-lobby.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gameserver-lobby.selectorLabels" . | nindent 4 }}

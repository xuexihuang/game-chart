apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pay-rpc.fullname" . }}-test-connection"
  labels:
    {{- include "pay-rpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pay-rpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
